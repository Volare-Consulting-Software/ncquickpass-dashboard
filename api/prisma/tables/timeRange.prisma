/// A time window within a day, in minutes-from-midnight local to the schedule timezone.
model TimeRange {
  id          String @id @default(cuid())
  dayId       String
  startMinute Int
  endMinute   Int

  day ScheduleDay @relation(fields: [dayId], references: [id], onDelete: Cascade)

  @@index([dayId])
}
